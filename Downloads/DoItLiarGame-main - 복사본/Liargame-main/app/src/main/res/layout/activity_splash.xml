<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_most_deep"
    tools:context=".activity.SplashActivity">

    <LinearLayout
        android:id="@+id/activity_splash_title"
        android:layout_width="350dp"
        android:layout_height="80dp"
        android:gravity="center"
        android:background="@drawable/app_title_background"
        android:layout_marginTop="200dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="300dp"
            android:layout_height="60dp"
            android:background="@drawable/app_title"/>
    </LinearLayout>

    <TextView
        android:id="@+id/activity_splash_start_button"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@+id/activity_splash_title"
        android:text="START"
        android:textSize="25dp"
        android:textStyle="bold"
        android:alpha="0"
        android:background="@drawable/start_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <TextView
        android:id="@+id/activity_splash_writer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Made By Name"
        android:textColor="@color/yellow"
        android:paddingHorizontal="20dp"
        android:paddingVertical="2dp"
        android:background="@drawable/writer_round"
        android:layout_marginBottom="50dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>